<nav class="navbar">
  <!-- Brand Logo -->
  <a href="/" class="navbar-brand">just-do-It</a>

  <!-- Navigation Links -->
  <div class="nav-links">
    
  </div>

  <!-- Conditional Button -->
  <% if (typeof isSignUpPage !== 'undefined' && isSignUpPage) { %>
    <button class="sign-in-button">
      <a href="/signin" style="color: white; text-decoration: none;">Sign In</a>
    </button>
  <% } else if (typeof isSignInPage !== 'undefined' && isSignInPage) { %>
    <button class="sign-up-button">
      <a href="/signup" style="color: white; text-decoration: none;">Sign Up</a>
    </button>
  <% } else { %>
    <button class="sign-out-button">
      <a href="/signout" style="color: white; text-decoration: none;">Sign Out</a>
    </button>
  <% } %>
  <!-- Debug output -->
  <div style="display: none;">
    isSignInPage: <%= typeof isSignInPage !== 'undefined' ? isSignInPage : 'undefined' %>
  </div>
</nav>

<style>
  /* Navbar Styling */
  .navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 10px;
    background-color: #234b5e;
    width: 100%;
    position: sticky;
    top: 0;
    z-index: 10;
  }

  /* Logo and Links */
  .navbar-brand {
    font-size: 1.8rem;
    font-weight: bold;
    color: #ffffff;
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: 20px;
  }

  .nav-link {
    color: #ffffff;
    text-decoration: none;
    font-size: 1.1rem;
    padding: 10px;
    position: relative;
  }

  .nav-link:hover::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 3px;
    background-color: #ff5722;
    bottom: 0;
    left: 0;
  }

  .sign-up-button,
  .sign-out-button {
    padding: 8px 15px;
    color: #ffffff;
    border: none;
    border-radius: 20px;
    cursor: pointer;
  }
  .sign-in-button,
  .sign-up-button,
  .sign-out-button {
    padding: 8px 15px;
    color: #ffffff;
    border: none;
    border-radius: 20px;
    cursor: pointer;
  }

  .sign-up-button {
    background-color: #4CAF50;
  }

  .sign-up-button:hover {
    background-color: #45a049;
  }

  .sign-in-button {
    background-color: #6a4caf;
  }

  .sign-in-button:hover {
    background-color: #7b45a0;
  }

  .sign-out-button {
    background-color: #ff5722;
  }

  .sign-out-button:hover {
    background-color: #e64a19;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .menu-toggle {
      display: block;
    }
  }
</style>
